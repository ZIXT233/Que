[package]
name = "que-hook"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true
panic = "abort"
